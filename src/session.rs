//! Session & conversation persistence.
//!
//! v0.1 stub: schema is bootstrapped in `sql/bootstrap.sql`, but the agent
//! loop is currently stateless per-call. Multi-turn sessions land in v0.2 —
//! see `docs/ROADMAP.md`.

// Intentionally empty for the first cut. Keeping the module so the public
// surface in `lib.rs` is stable as we grow into multi-turn.
